.aider*
.env
build/
dist/
.vscode
.DS_Store
.dev/

# UI build output; placeholder.txt is kept so the go:embed succeeds.
internal/server/ui_dist/*
.claude/
bin/
Makefile.bak
logs/
*.log

# Private companion repo (deploy topology docs) — must never be committable
# from inside this working tree.
docs-skein/
