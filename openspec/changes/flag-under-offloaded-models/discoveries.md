- perf_class is the highest-severity item: ModeCustom hardcodes native-gpu (placement.go:153), disabling opencode-skein's HOST_PACED_PENALTY (placement.ts:104-107) for every pinned model. Verified on host A: native-gpu reported at 1.2 tok/s with 7165 MB host-resident.
- Counterfactual validated by measurement: removing (not raising) the pins gave 5.42 -> 39.42 and 4.04 -> 35.05 tok/s, and the planner chose full GPU residency unaided with placement.applied=true. All six host A models now run_mode=gpu, host_resident_mb=0.
- Two API defects block the recommended remedy: no removal path for n_gpu_layers (0 would write --n-gpu-layers 0), and GET /api/models/config/{id} returns --port resolved instead of ${PORT}, so read-modify-write hardcodes a dynamic port. Tasks 17-18.
- Fleet audit: placement pins exist on every model on every host (host A 6x40, host B 4x99, host C 1x99 + 5x999999). under_offloaded must NOT fire for all-layers pins on models that fit, or it alerts fleet-wide on day one. See task 12b and #24.
