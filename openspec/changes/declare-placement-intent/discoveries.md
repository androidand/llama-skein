- Generator investigation resolved: NOT generative in the shipping binary. internal/server.buildCmd (modelhelpers.go:322-337) already emits no placement flag and its comment records this exact bug being fixed before (deepseek --n-cpu-moe 25 --ctx-size 32768 incident). Live install paths pass flags="" or operator-supplied flags. No generator fix needed.
- Pre-fix buildCmd survives at proxy/proxymanager_config.go:526-572 with both defects: clones the template model's entire flag set including --model-draft and --mmproj from an unrelated model (comment claims it stops at --model; the loop does not), and falls back to -ngl 99. Unreachable — proxy.New only in cmd/legacy, not in the Makefile — but a landmine. Task 18.
- Fleet scope: every model on every host carries a placement pin (host A 6x40, host B 4x99, host C 1x99 + 5x999999), so placement.mode=auto is unreachable fleet-wide and add-auto-hybrid-placement can never engage. 99/999999 are not harmless: they disable hybrid split, --fit-target reserve, and the retry ladder. host B/host C figures are repo copies, unverified live.
- M1 on host A is the motivating case: -ngl 99 + ctx 139264 = 32.6 tok/s but planner disabled and preload refused; unpinned = 14.7 tok/s; unpinned + ctx 110592 = still marginal. Every option is wrong. The intent 'spend the safety reserve on layers, keep all of them on GPU' is one sentence and currently inexpressible except as a raw pin with three unrelated side effects.
